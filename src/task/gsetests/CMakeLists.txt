IF ( CMAKE_CONFIGURATION_TYPES )
	SET( SRC ${SRC}
		$<$<OR:$<CONFIG:Debug>,$<CONFIG:FastDebug>>:${PWD}/GSETests.cpp>
		PARENT_SCOPE )
ELSE ()
	SET( SRC ${SRC}
		${PWD}/GSETests.cpp
		PARENT_SCOPE )
ENDIF ()
