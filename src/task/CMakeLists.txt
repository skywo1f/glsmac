SUBDIR( main )

IF ( CMAKE_CONFIGURATION_TYPES OR CMAKE_BUILD_TYPE STREQUAL "Debug" OR CMAKE_BUILD_TYPE STREQUAL "FastDebug" )
	SUBDIR( gseprompt )
	SUBDIR( gsetests )
ENDIF ()

SET( SRC ${SRC}

	PARENT_SCOPE )
